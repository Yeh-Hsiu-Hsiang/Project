<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clothes">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/今天穿甚麼"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Manageclothes"
            android:label="@string/管理衣服" />
        <activity android:name=".Weekweather"
            android:label="@string/天氣詳細" />
        <activity
            android:name=".AddSchedule"
            android:label="@string/新增活動" />
        <activity
            android:name=".setting"
            android:label="@string/其他設定" />
        <activity
            android:name=".AddClothes"
            android:label="@string/新增衣服" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>